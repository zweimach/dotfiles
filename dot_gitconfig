[user]
email = zweimach@zweimach.org
name = Ananda Umamil
signkey = 163CE4CEA7F464B6

[credential]
helper = store

[color]
ui = auto

[core]
editor = nvim
excludesfile = /home/zweimach/.gitignore
pager = delta

[delta]
line-numbers = true
side-by-side = true
syntax-theme = Dracula

[diff]
colorMoved = default

[gpg]
program = gpg

[commit]
gpgsign = true

[filter "lfs"]
smudge = git-lfs smudge -- %f
process = git-lfs filter-process
required = true
clean = git-lfs clean -- %f

[log]
mailmap = true

[pull]
ff = only

[init]
defaultBranch = main

[pretty]
pretty-oneline = "format:%C(auto)%h%d %s %Cblue[%cN] %Cgreen[%cr]"

[alias]
c = commit
bc = branch
d = diff
dc = diff --cached
go = checkout
l = log
lp = log --pretty=pretty-oneline
lg = log --pretty=pretty-oneline --all --graph
ll = log --pretty=pretty-oneline --numstat
rb = rebase
re = remote
s = status
ss = status -s
